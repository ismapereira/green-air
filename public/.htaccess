<IfModule mod_rewrite.c>
    RewriteEngine On
    # Sem RewriteBase fixo - funciona em qualquer caminho (ex: /Desenvolvimentos/green-air/public/)
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>
